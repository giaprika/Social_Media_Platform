# api/proto/buf.yaml
version: v1
name: buf.build/extaseq/chat-service # (Có thể đặt tên tùy ý cho local)
deps:
  - buf.build/googleapis/googleapis # <-- Quan trọng: Tự tải google api
  # - buf.build/grpc-ecosystem/grpc-gateway # <-- For OpenAPI annotations
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT