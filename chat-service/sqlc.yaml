version: "2"
sql:
  - schema: "migrations"
    queries: "internal/repository/queries"
    engine: "postgresql"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        sql_package: "pgx/v5" # Sử dụng pgx driver cho hiệu năng cao
        emit_json_tags: true