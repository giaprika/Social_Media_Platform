# Notification Service Configuration

# Server Port
PORT=8002

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=notification_db
DB_USER=postgres
DB_PASSWORD=binh39
DB_MAX_CONNECTIONS=10
DB_SSL=false

# Redis Configuration (Optional - có thể comment nếu không dùng)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# RabbitMQ Configuration (Optional - có thể comment nếu không dùng)
# RABBITMQ_URL=amqp://localhost:5672

# Environment
NODE_ENV=development
